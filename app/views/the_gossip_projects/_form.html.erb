<%= form_with(model: the_gossip_project) do |form| %>
  <% if the_gossip_project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(the_gossip_project.errors.count, "error") %> prohibited this the_gossip_project from being saved:</h2>

      <ul>
        <% the_gossip_project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
