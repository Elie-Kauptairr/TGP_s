<div class="container">
  <h2><%= @user.first_name %> <%= @user.last_name %></h2>
  <p><strong>Description :</strong> <%= @user.description %></p>
  <p><strong>Email :</strong> <%= @user.email %></p>
  <p><strong>Âge :</strong> <%= @user.age %></p>
  <p><strong>Ville :</strong> <%= @user.city.name %></p>
  <p><strong>Ville :</strong> <%= link_to @user.city.name, city_path(@user.city), class: "text-primary" %></p>


  <div class="mt-3">
    <%= link_to "Retour aux potins", the_gossip_projects_path, class: "btn btn-secondary" %>
  </div>
</div>
